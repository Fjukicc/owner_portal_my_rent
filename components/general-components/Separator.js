import { View } from "react-native";
import React from "react";

const Separator = () => {
  return (
    <View
      style={{
        marginVertical: 16,
        borderColor: "gray",
        borderWidth: 0.3,
      }}
    />
  );
};

export default Separator;
